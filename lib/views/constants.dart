import 'package:flutter/cupertino.dart';

/// Default height for Movie 's Poster image.
const double kMoviePosterHeight = 325;

/// Default height for Movie Poster 's Detail body data - the bottom part below Poster image.
const double kMoviePosterBodyHeight = 100;

/// Default width for Movie 's Poster image.
const double kMoviePosterWidth = 210;

/// Default size for Movie 's small Poster for MovieDetailTileWidget.
const double kMoviePosterSmallHeight = 160;
const double kMoviePosterSmallWidth = 120;

/// Default size for Cinema 's Poster image.
const double kCinemaPosterSize = kMoviePosterWidth;

/// Default size for Cinema 's small Poster in CinemaTileWidget.
const double kCinemaPosterSmallHeight = 75;
const double kCinemaPosterSmallWidth = 100;

/// Default height for Cinema Poster 's Detail body data - the bottom part below Poster image.
const double kCinemaPosterBodyHeight = 50;

const double kImageBorderRadius = 8;

const double kAvatarRadius = 70;

/// Lotte logo size
const double kLogoSize = 32;

/// Padding value default
const double kPaddingDefault1 = 4;
const double kPaddingDefault2 = 8;
const double kPaddingDefault3 = 12;
const double kPaddingDefault4 = 16;

const EdgeInsets kEdgeInsetsDefault1 = EdgeInsets.symmetric(
  horizontal: kPaddingDefault3,
  vertical: kPaddingDefault2,
);

/// Duration for animation
const Duration kAnimationDuration = Duration(milliseconds: 250);

/// Empty placeholder for text.
const String empty = '';

/// Text height and width replica for Shimmer effect.
const double kTextHeight = 16;
const double kTextWidth = 160;

/// Button height for placehodler Shimmer effect.
const double kButtonHeight = 36;

/// Small icon size.
const double kSmallIconSize = 18;

/// Divider thickness size.
const double kDividerSize2 = 2;
const double kDividerSize4 = 4;
const double kDividerSize12 = 12;

/// Divider 's indent size.
const double kIndentSize = 32;
